{{>header}}
{{>nav}}
<section class="container my-5">
    <h2>Edit Task</h2>
    <form action="/editLogic/{{task.id}}">
        <div class="mb-3">
            <input name="title" class="form-control" placeholder="{{task.title}}">
        </div>
        <div class="mb-3">
            <input name="content" class="form-control" placeholder="{{task.content}}">
        </div>
        <div class="mb-3">
            <button class="btn btn-primary">Edit task</button>
        </div>
    </form>
</section>
{{>footer}}